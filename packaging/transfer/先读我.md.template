# 微信飞书消息分析 Dashboard

这是“微信飞书消息分析 Dashboard”v{{VERSION}} 的 macOS 完整项目移交包，对应 Git 提交 `{{COMMIT}}`。

包内包含完整项目源码、锁定依赖清单、Codex / Claude Code Skill、安装和检查脚本，以及微信与飞书首次接入说明。它不包含任何聊天正文、联系人、群名、账号目录、数据库、密钥、密码、OAuth 令牌、Keychain 内容、日志或本机缓存。

## 最快安装方法

1. 把整个“微信飞书消息分析 Dashboard”文件夹放到准备长期保留的位置，例如 `~/Documents/微信飞书消息分析 Dashboard`。安装后不要移动其中的项目文件夹，因为 Agent Skill 会链接到该目录。
2. 确认微信 Mac 版已经正常登录。安装过程不会退出、重启、重签或修改微信。
3. 双击 `安装并启动.command`。如果 macOS 阻止直接运行，可以右键该文件并选择“打开”。
4. 安装结束后，按照 `首次连接双端.md` 分别检查微信读取器与飞书用户授权。
5. 在 Codex 输入 `$wechat-dashboard`；Skill 会启动只监听本机的临时 Dashboard，并打开 `/setup`。

## 核心边界

- 微信和飞书均为只读；项目没有发送、回复、撤回或修改消息的功能。
- 飞书固定使用 `lark-cli --as user`；bot 身份不会成为静默回退。
- 两端消息和分析结果加密保存在本机，主密钥位于当前 Mac 的 Keychain。
- 私信只有在对应平台设置中明确开启后，才会进入当前 Codex 任务的受限分析上下文。
- 语义分析只接受 Terra High；其他模型或推理强度的结果会被拒绝。
- 页面打开期间约 30 分钟增量同步一次；项目不安装 Automation、cron、LaunchAgent 或常驻 AI 任务。

## 目录

- `wechat-feishu-message-analysis-dashboard/`：完整项目源码和 Skill。
- `安装并启动.command`：安装依赖、飞书 CLI、构建项目、安装 Skill 并启动本机页面。
- `检查安装.command`：只读检查当前 Mac 的安装与授权状态。
- `首次连接双端.md`：微信与飞书逐机接入步骤。
- `移交清单.md`：版本、组件、排除项和安全边界。
- `RELEASE_MANIFEST.json`：产品版本、Git 提交与生成时间。
- `SHA256SUMS.txt`：包内所有文件的 SHA-256 清单。
