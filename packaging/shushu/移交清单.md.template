# 移交清单

## 版本

- 产品：WeChat Dashboard
- Release：v{{VERSION}}
- Git 提交：`{{COMMIT}}`
- 目标系统：macOS
- 前端与本地服务：Next.js 16.2.6、React 19.2.4、TypeScript
- 数据库：SQLite / better-sqlite3 12.x
- 微信读取器：`@jackwener/wx-cli` 0.3.0，锁文件同时包含 macOS arm64 与 x64 包
- 包管理器：pnpm，依赖版本由 `pnpm-lock.yaml` 固定

## 已包含

- Dashboard 全部受版本控制的源码。
- `skills/wechat-dashboard` Codex / Claude Code Skill。
- `package.json` 与 `pnpm-lock.yaml`。
- 本地加密、同步、会话租约、搜索、星标、关键词优先级、逐群汇总和重点关注功能。
- Lens Design · Slate & Wine Modernist 界面。
- 每次调用 Skill 自动打开本机 Dashboard；停止命令例外。
- 安装、权限、读取器检查、打包与验证脚本。
- README、隐私、安全和协作文档。

## 主动排除

- `.git/`
- `node_modules/`
- `.next/`
- `.env` 与 `.env.local`
- `.local-debug/`
- 个人项目 handoff 和机器调查记录
- `~/.wechat-dashboard/` 数据库、配置、备份与日志
- `~/.wx-cli/` 密钥、缓存与 daemon 状态
- 微信数据库、聊天正文、联系人、账号目录、密码与 Keychain 内容
- 临时分析 context / result 文件
- UI mockup 原始 ZIP

`node_modules` 和 `.next` 没有打包，因为它们包含绑定原机器 Node 版本与 CPU 架构的原生模块和缓存。对方 Mac 会依据锁文件重新安装并编译，这是更可靠的迁移方式。

## 安全边界

- 本地服务只监听 `127.0.0.1`。
- 写入 API 拒绝跨来源请求。
- Dashboard 敏感字段使用 AES-256-GCM，本机主密钥保存在当前用户 macOS Keychain。
- Agent 只接收选定的群聊上下文；私信不会进入模型分析。
- 没有常驻服务、自动化或后台 AI 调度。
