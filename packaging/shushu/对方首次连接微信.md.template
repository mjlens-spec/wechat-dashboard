# 对方首次连接微信

## 1. 先完成代码与 Skill 安装

运行同级目录下的 `安装并启动.command`。脚本会：

1. 检查 macOS、Node.js 20+ 与 pnpm。
2. 严格按照 `pnpm-lock.yaml` 安装依赖。
3. 为当前 Mac 重建 `better-sqlite3`。
4. 收紧 `~/.wechat-dashboard` 与 `~/.wx-cli` 的权限。
5. 构建 production Dashboard。
6. 把 `skills/wechat-dashboard` 链接到当前用户的 Codex 与 Claude Code Skill 目录。
7. 启动只监听 `127.0.0.1` 的临时服务并打开 `/setup`。

## 2. 真实数据为什么需要逐机配置

微信数据库、数据库密钥和账号目录都与当前 Mac、当前微信版本和当前登录账号相关。移交他人的 `all_keys.json`、Dashboard 数据库或 Keychain 主密钥既不安全，也通常无法工作。因此，本包只提供读取框架与检查工具，不携带任何人的真实微信数据。

## 3. 不要执行的操作

- 不要高频退出或重新登录微信。
- 不要反复重启微信。
- 不要从其他用户复制 `~/.wx-cli/all_keys.json`。
- 不要直接运行 `wx init` 覆盖现有状态。
- 不要修改微信签名、附加 Frida 或开启永久调试权限，除非使用者明确决定进行一次单独的读取器维护并理解风险。
- 不要把聊天正文、数据库或密钥提交到 GitHub。

## 4. 推荐的接入顺序

1. 保持微信处于当前正常登录状态。
2. 运行 `安装并启动.command`。
3. 在 `/setup` 检查 `wx-cli`、真实数据、读取服务、缓存权限和当前账号。
4. 如果 `wx-cli` 已安装但“真实数据”未就绪，在 Codex 中使用 `先读我.md` 里的建议 Prompt，只检查这台 Mac 的读取器状态。
5. 读取器就绪后，在 Codex 调用 `$wechat-dashboard`，或在 Claude Code 调用 `/wechat-dashboard`。
6. 首轮只建立会话目录与最近消息快照，再逐批补齐当天群聊。

## 5. 常用命令

在 `WeChat-Dashboard` 目录执行：

```bash
pnpm reader:inspect
pnpm privacy:harden
pnpm build
pnpm skill:check
pnpm session:start
pnpm session:status
pnpm session:stop
```

如果 `better-sqlite3` 无法加载：

```bash
pnpm rebuild better-sqlite3
pnpm build
```

## 6. 成功标准

- `/setup` 显示读取器、真实数据、读取服务、缓存权限和当前账号均就绪。
- Dashboard 能列出本机群聊并完成一次增量同步。
- Codex 或 Claude Code 能调用 Skill，逐群生成汇总和重点关注提示。
- 关闭 Dashboard 页面后，本机临时服务会在短时间内退出。
